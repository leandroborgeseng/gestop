# Frontend (Next.js)
NODE_ENV=development
PORT=3000

# URL interna do backend (Railway rede privada)
# BACKEND_INTERNAL_URL=http://${{gestop.RAILWAY_PRIVATE_DOMAIN}}:${{gestop.PORT}}

# Exibir credenciais demo no login (somente desenvolvimento local)
NEXT_PUBLIC_SHOW_DEMO_LOGIN=true

# NAO definir em producao — quebra login no browser
# NEXT_PUBLIC_API_URL=
